'use strict';

const chakram = require('chakram');
const expect = chakram.expect;

describe('Encryption', () => {

  describe('#error', () => {

    it('should fail if public key is empty', done => {
      done();
    });

    it('should fail if public key is null', done => {
      done();
    });

    it('should fail if public key is invalid', done => {
      done();
    });

  });

  describe('#success', () => {
    
    it('should encrypt password with generated public key', done => {
      done();
    });

  });

});